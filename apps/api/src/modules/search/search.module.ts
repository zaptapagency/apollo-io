import { Module } from '@nestjs/common';

// Workstream A fills this in: company/contact CRUD, lead search (OpenSearch-backed),
// saved searches, lists, tags, CSV import/export.
@Module({})
export class SearchModule {}

import { Module } from '@nestjs/common';

// Workstream E fills this in: auth (signup/signin/session), org/membership management,
// API keys, audit log, Stripe billing, webhooks dispatch.
@Module({})
export class PlatformModule {}

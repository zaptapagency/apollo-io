import { Module } from '@nestjs/common';

// Workstream B fills this in: pluggable enrichment provider adapters (mock/Clearbit-style/
// Hunter-style), email verification pipeline, BullMQ enrichment queue + worker.
@Module({})
export class EnrichmentModule {}

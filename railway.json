{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "./apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'cd /repo/packages/db && npx prisma migrate deploy && node /repo/apps/api/dist/main.js'",
    "restartPolicyMaxRetries": 5
  }
}
